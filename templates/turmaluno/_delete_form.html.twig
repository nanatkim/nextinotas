<form method="post" action="{{ path('turmaluno_delete', {'id': turmaluno.id,'facul':facul}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ turmaluno.id) }}">
    <button class="btn">Delete</button>
</form>