{% extends 'base.html.twig' %}

{% block title %}TurmAluno{% endblock %}

{% block body %}
    <h1>TurmAluno</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ turmaluno.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('turmaluno_index') }}">back to list</a>

    <a href="{{ path('turmaluno_edit', {'id': turmaluno.id}) }}">edit</a>

    {{ include('turmaluno/_delete_form.html.twig') }}
{% endblock %}