{% extends 'base.html.twig' %}

{% block title %}Disciplina{% endblock %}

{% block body %}
    <h1>Disciplina</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disciplina.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ disciplina.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('disciplina_index') }}">back to list</a>

    <a href="{{ path('disciplina_edit', {'id': disciplina.id}) }}">edit</a>

    {{ include('disciplina/_delete_form.html.twig') }}
{% endblock %}