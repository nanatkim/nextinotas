{% extends 'base.html.twig' %}

{% block title %}Disciplina{% endblock %}

{% block body %}
    <a class="btn btn-default text" href="{{ path('disciplina_index',{'facul':faculdade.id}) }}">Voltar</a>
    <hr>
    <div class="row text">
        <div class="col-md-6 col-md-offset-3">
            <div class="panel panel-primary">
                <div class="panel-heading">Disciplina</div>
                <div class="panel-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn">{{ button_label|default('Save') }}</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}