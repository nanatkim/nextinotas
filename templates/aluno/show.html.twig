{% extends 'base.html.twig' %}

{% block title %}Aluno{% endblock %}

{% block body %}
    <h1>Aluno</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ aluno.id }}</td>
        </tr>
        <tr>
            <th>Nome</th>
            <td>{{aluno.nome }}</td>
        </tr>
        <tr>
            <th>Semestre</th>
            <td>{{ aluno.semestre }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('aluno_index',{'facul':faculdade.id}) }}">back to list</a>

    <a href="{{ path('aluno_edit', {'id': aluno.id,'facul':faculdade.id}) }}">edit</a>

    {{ include('aluno/_delete_form.html.twig') }}
{% endblock %}