{% extends 'base.html.twig' %}

{% block title %}AvAluno{% endblock %}

{% block body %}
    <a class="btn btn-default text" href="{{ path('avbase_show',{'facul':faculdade.id,'tur':turma.id,'id':avbase.id}) }}">Voltar</a>
    <hr>
    <div class="row text">
        <div class="col-md-6 col-md-offset-3">
            <div class="panel panel-primary">
                <div class="panel-heading">{{ aluno.idAluno.idAluno.nome }}  <div class="pull-right">| Nota máxima: {{ avbase.notaMax }}</div></div>
                <div class="panel-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}