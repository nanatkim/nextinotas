{% extends 'base.html.twig' %}

{% block title %}AvBase{% endblock %}

{% block body %}
    <a class="btn btn-default text" href="{{ path('avbase_index',{'facul':faculdade.id, 'tur':turma.id}) }}">Voltar</a>
    <hr>
    <div class="row text">
        <div class="col-md-6 col-md-offset-3">
            <div class="panel panel-primary">
                <div class="panel-heading">Avaliação</div>
                <div class="panel-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}